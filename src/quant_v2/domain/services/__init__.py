"""领域服务 —— 纯计算，零 I/O、零外部依赖。

这里放的是"算得对不对可以被单测完全覆盖"的那部分逻辑：
复权换算、成本求值、数据指纹、生命周期迁移表、PIT 池规则、数据质量规则。

**不放**任何需要碰文件 / 网络 / 数据库的东西 —— 那些属于 `adapters/`。
"""

from __future__ import annotations

__all__: list[str] = []
