# quant-research 编码规范

## 基本规则

1. 所有新增函数必须写中文注释。
2. 中文注释至少说明函数用途、关键输入和返回结果。
3. 外部依赖必须通过适配层接入，不允许策略直接依赖供应商 SDK。
4. 新增策略必须通过统一策略接口接入，不允许直接在策略内部下单。
5. 风控逻辑必须集中在风控模块，不允许散落在策略内部。

## 命名约定

- 目录名和模块名保持语义清晰。
- 如果实现读取 CSV，本地适配器命名应体现 CSV，而非 parquet。
- 回测、纸盘、实盘尽量复用统一领域模型。
