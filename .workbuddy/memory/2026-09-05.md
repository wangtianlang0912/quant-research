# 2026-09-05 工作日志

## 从 GitHub 克隆最新代码到本地
- **需求**: 用户要求「下载最新文件到本地」，确认源为 `git@github.com:wangtianlang0912/quant-research.git`，保存目录 `~/Documents/code/`
- **SSH 不可用**: `ssh -T git@github.com` 返回 `Permission denied (publickey)`，本机 `~/.ssh/` 仅有 known_hosts，**无任何密钥对**
- **解决方案**: 仓库 HTTPS 公开可达（`curl` 返回 200），改用 `git clone https://github.com/wangtianlang0912/quant-research.git`
- **结果**: 克隆成功 → `~/Documents/code/quant-research`（700 objects / 15.95 MiB / 328 文件 / 35M）
- **最新版本**: HEAD = `7b2bdb3`（2026-09-05 23:04:02 +0800）「chore: 提交本地全部改动（推送追踪、因子扫描器、仪表盘、板块轮动/资金流向模块、每日报告与图表）」，79 files changed
- **分支**: main（另有 3 个 copilot/* 远程分支）

## 重要发现
- 远端版本比当前工作区 `/Users/leon/WorkBuddy/Claw/quant-research` **更新**：多出 17 个顶层文件
  （`daily_tracking_review_2026-09-02.md`、`global_flow_2026-07-22.md`、`daily_tracking_2026-08-07/08-12.md`、
  `dashboard_rewrite_20260721T1747.md`、`lenovo-style_picks_2026-07-27.md` 等）
- 新增关键模块：`src/dashboard/global_flow.py`(361行)、`src/dashboard/sector_rotation.py`(247行)、
  `src/scanners/factor_scanner.py` 大幅改动、`visualization/templates/dashboard.html` 重写(+1376行)
- 工作区数据缓存（data/cache/klines，617 个文件）仍停在 2026-09-01，未同步最新

## 待办
- 如需 SSH 推送：需生成 ed25519 密钥并添加到 GitHub（`ssh-keygen -t ed25519` + `gh ssh-key add`）
- 是否将远端新版本合并回工作区，待用户决定

---

## 运行可视化面板 + 修复缺失模块（同日晚）

### 启动
- 入口 `visualization/app.py`（FastAPI），端口 8899，托管 Python 用 managed venv
- 补装依赖：`jinja2` 3.1.6、`matplotlib`（耗时较久，Python 3.13 环境）

### 致命问题：`src/data/` 从未入库
- `.gitignore` 写的是 `data/`（无前导斜杠）→ 匹配任意层级 data 目录 → 把 **源码目录 `src/data/` 一起忽略**
- 导致 `src.data.akshare_client` 缺失，10 个模块 import 失败，`/api/ai-heat`、`/api/counter-sector`、
  `/api/asia-chart` 三个端点全部 500
- **已修复** `.gitignore`：`data/` → `/data/`，`src/data/` 现已可正常提交

### 重建 `src/data/akshare_client.py`（腾讯行情客户端）
- 从 10 个调用方反推接口：`TencentClient` / `StockQuote` / `KlineBar`
- 实测踩坑记录已写入 `MEMORY.md`（K线是 O-C-H-L 顺序、港股无字段偏移、成交额单位不统一等）
- 自测通过：寒武纪 -2.54%/90.48亿、腾讯控股 +2.26%/106.65亿/PE16.2、恒指 +1.74%/2768.57亿

### 验证结果
- 7 个调用方模块（alerts/dashboard/scanners）全部 import 成功
- API 端点：8/9 正常，`/api/asia-chart` 曾因缺 matplotlib 500，装完待复测
- 业务数据抽样：AI热度 38 只标的全取到（热度 15.2 冰点）；对手盘 43/43 只、8 板块（养殖/农业 +5.08% 领涨）

### 遗留
- `/api/symbols` 返回 `[]`：`data/1d/` 无 CSV 行情文件（被 gitignore 排除）→ 前端 K 线图表区空
- `pytest` 未安装，`tests/`（11 个测试）尚未运行
